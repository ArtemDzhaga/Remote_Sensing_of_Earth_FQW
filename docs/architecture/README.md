# Архитектура (Structurizr)

Файл `workspace.dsl` открывается в Structurizr Lite / Cloud или экспортируется через CLI.

Согласован с кодом: см. пакет `src/dem/`, контейнер качества (`dem.viz.validate_dem`), хранилище — локальный каталог `outputs/` (+ legacy `data/`).
